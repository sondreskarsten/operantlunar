% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{convergence_demo}
\alias{convergence_demo}
\title{Two-pipelines convergence demonstration}
\usage{
convergence_demo(
  true_function = "escape",
  arms = c("attention", "escape", "tangible", "goal"),
  n_sessions_grid = c(6L, 10L, 20L),
  n_seeds_grid = c(1L, 3L),
  keep_set = c("first", "best", "pool"),
  seed_offsets = c(0L, 100L, 200L),
  n_protocol_subjects = 16L,
  p_reinforce = 0.3,
  p_noise = 0.05,
  response_cost = 0.1,
  session_len = 100L
)
}
\arguments{
\item{true_function, arms}{Environment.}

\item{n_sessions_grid}{Session counts for the ad hoc pipeline.}

\item{n_seeds_grid}{Seed counts for the ad hoc pipeline.}

\item{keep_set}{Collapse rules for the ad hoc pipeline.}

\item{seed_offsets}{Seed offsets applied to both pipelines (subject-pool choice).}

\item{n_protocol_subjects}{Subject cap for the protocol pipeline.}

\item{p_reinforce, p_noise, response_cost, session_len}{Environment/run parameters.}
}
\value{
A list with \code{results} (tibble), \code{adhoc_distinct}, \code{protocol_distinct}, \code{protocol_verdict}.
}
\description{
The headline validation of the protocol-as-method. It runs the undisciplined
\code{\link[=adhoc_fa]{adhoc_fa()}} across a grid of researcher choices (session count, seed count,
collapse rule, seed offset) and the disciplined \code{\link[=functional_analysis_replicated]{functional_analysis_replicated()}}
across the same seed offsets, and reports the verdict each yields. The ad hoc
verdict varies with the knobs; the protocol verdict does not. The residual
variability of each is quantified as the number of distinct verdicts produced.
}
