% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gym_toolkit.R
\name{gym_dra}
\alias{gym_dra}
\title{Differential reinforcement (DRA) on a Gymnasium-style gridworld}
\usage{
gym_dra(
  arms = c("problem", "alternative"),
  corridor = 3L,
  agent = "q_learning",
  n_baseline = 400L,
  n_treatment = 600L,
  window = 50L,
  max_steps = 20L,
  seed = 0L
)
}
\arguments{
\item{arms}{Two channel names, \code{c(problem, alternative)}.}

\item{corridor}{Arm length.}

\item{agent}{Registry key for the agent.}

\item{n_baseline, n_treatment}{Episodes per phase.}

\item{window}{Episodes per summary window.}

\item{max_steps}{Step cap per episode.}

\item{seed}{Seed.}
}
\value{
A tibble with \code{window_mid}, \code{arm}, \code{reach_rate}, \code{switch_ep}.
}
\description{
Trains the agent to navigate to a problem arm (baseline), then switches the
active channel so the problem arm is on extinction and an alternative arm is
reinforced (treatment). Behaviour reallocates from the problem arm to the
alternative: the contingency engine behind DRA/FCT on a multi-state navigation
task.
}
\examples{
\donttest{
gym_dra()
}
}
