% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lunarlander.R
\name{lunar_adhoc_solved}
\alias{lunar_adhoc_solved}
\title{Ad hoc "is it solved?" verdict from a short evaluation}
\usage{
lunar_adhoc_solved(
  returns = lunar_returns(),
  policy_seed = 0L,
  n_episodes = 10L,
  pool = 1L,
  n_pools = 10L,
  threshold = 200
)
}
\arguments{
\item{returns}{Returns tibble.}

\item{policy_seed}{Policy to evaluate.}

\item{n_episodes}{Episodes sampled.}

\item{pool}{Which terrain pool (1-indexed).}

\item{n_pools}{Number of contiguous pools the terrains are split into.}

\item{threshold}{Solved threshold.}
}
\value{
A list with \code{mean}, \code{verdict}.
}
\description{
Takes \code{n_episodes} from one contiguous terrain pool and declares the policy
solved if that short-sample mean clears \code{threshold}. The verdict depends on
how many episodes and which pool were chosen, which is the researcher degree
of freedom the protocol removes.
}
