% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lunarlander.R
\name{lunar_steady_state_return}
\alias{lunar_steady_state_return}
\title{Steady-state evaluation estimate for one policy}
\usage{
lunar_steady_state_return(
  returns = lunar_returns(),
  policy_seed = 0L,
  block = 10L,
  min_blocks = 7L,
  k = 6L,
  tol_trend = 8,
  tol_bounce = 15
)
}
\arguments{
\item{returns}{Returns tibble (defaults to the bundled dataset).}

\item{policy_seed}{Policy to evaluate.}

\item{block}{Episodes per block.}

\item{min_blocks}{Minimum blocks before the stability rule applies.}

\item{k}{Window length for stability and the read.}

\item{tol_trend, tol_bounce}{Stability tolerances on the return scale.}
}
\value{
A list with \code{estimate}, \code{stable}, \code{n_blocks}, \code{n_episodes}, \code{series}.
}
\description{
Accumulates episodes in terrain order, tracks the cumulative mean return after
each block, and applies the steady-state stability rule to the cumulative-mean
series: the estimate is read once the running mean has settled (no trend and
bounded range over the last \code{k} blocks), or non-stabilisation is reported. A
short evaluation is read off the unsettled head of this same series and is
therefore unreliable; the settled estimate is what the protocol reports.
}
