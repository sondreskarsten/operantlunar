% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gym_toolkit.R
\name{gridworld_env}
\alias{gridworld_env}
\title{Composite-reward gridworld (Gymnasium-style)}
\usage{
gridworld_env(
  arms = c("attention", "escape", "tangible", "goal"),
  corridor = 3L,
  magnitude = 1,
  step_cost = 0.02
)
}
\arguments{
\item{arms}{Channel names, one per arm.}

\item{corridor}{Steps from hub to an arm end.}

\item{magnitude}{Terminal reinforcement magnitude.}

\item{step_cost}{Per-step cost (always applied).}
}
\value{
A channel environment with \code{reset}, \code{step}, \code{n_actions}, \code{arms}, \code{n_states}.
}
\description{
A hub-and-spoke navigation task: from the hub, each action commits toward one
arm; staying on an arm advances to its end, choosing another arm retreats.
Reaching an arm's end terminates the episode and emits that arm's
reinforcement channel; every step costs \code{step_cost}. With \code{\link[=contingency_env]{contingency_env()}}
the active channel determines which arm pays, so the agent navigates to the
reinforced arm.
}
