% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{fa_stochastic_env}
\alias{fa_stochastic_env}
\title{Stochastic functional-analysis channel environment}
\usage{
fa_stochastic_env(
  true_function = "escape",
  arms = c("attention", "escape", "tangible", "goal"),
  p_reinforce = 0.8,
  p_noise = 0.05,
  magnitude = 1,
  response_cost = 0.1
)
}
\arguments{
\item{true_function}{Hidden maintaining channel.}

\item{arms}{Channels tested.}

\item{p_reinforce}{Probability an engage delivers the true-channel reinforcer.}

\item{p_noise}{Probability an engage misfires onto a uniformly random channel.}

\item{magnitude}{Reinforcer magnitude.}

\item{response_cost}{Cost per engage (makes withholding the default).}
}
\value{
A channel environment with \code{reset}, \code{step}, \code{n_actions}, \code{arms}.
}
\description{
A single-state engage/withhold environment (action 1 = engage, 2 = withhold)
whose engage response emits reinforcement on a hidden \code{true_function} channel
only probabilistically (\code{p_reinforce}), with occasional misfires onto a random
channel (\code{p_noise}). The stochasticity is deliberate: deterministic
contingencies make replication across subjects vacuous, so genuine
subject-to-subject variation (and occasional undifferentiated subjects)
requires within-condition randomness. Combine with \code{\link[=contingency_env]{contingency_env()}}: the
active channel determines which condition is in force.
}
