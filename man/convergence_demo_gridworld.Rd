% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{convergence_demo_gridworld}
\alias{convergence_demo_gridworld}
\title{Two-pipelines convergence demonstration on the procedural gridworld}
\usage{
convergence_demo_gridworld(
  true_function = "escape",
  arms = c("attention", "escape", "tangible", "goal"),
  env_seed = 1L,
  episodes_per_session = 15L,
  max_steps = 40L,
  size = 6L,
  n_walls = 6L,
  slip = 0.05,
  step_cost = 0.04,
  n_sessions_grid = c(12L, 28L),
  n_seeds_grid = c(1L, 3L),
  keep_set = c("first", "pool"),
  seed_offsets = c(0L, 50L, 100L),
  n_protocol_subjects = 12L,
  min_sessions = 22L,
  max_sessions = 45L,
  k = 10L
)
}
\arguments{
\item{true_function, arms, env_seed, episodes_per_session, max_steps, size, n_walls, slip, step_cost}{Gridworld parameters.}

\item{n_sessions_grid, n_seeds_grid, keep_set, seed_offsets, n_protocol_subjects}{Grid of researcher choices.}

\item{min_sessions, max_sessions, k}{Protocol steady-state parameters.}
}
\value{
The list returned by the internal grid runner: \code{results}, \code{adhoc_distinct}, \code{protocol_distinct}, \code{protocol_verdict}.
}
\description{
The headline demonstration on the navigation substrate. Because navigation is
slow to learn, an ad hoc pipeline with an arbitrary session budget reaches a
conclusion that depends on that budget (under-trained subjects look
undifferentiated), while the protocol's steady-state stopping detects
non-convergence and keeps training, so its verdict is invariant to the budget
and seed pool. The apparatus (\code{env_seed}) is held constant across both.
}
