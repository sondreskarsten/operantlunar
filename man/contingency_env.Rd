% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gym_toolkit.R
\name{contingency_env}
\alias{contingency_env}
\title{Contingency wrapper for any reinforcement environment}
\usage{
contingency_env(base_env, active = NULL)
}
\arguments{
\item{base_env}{An environment returning \code{channels} from \code{step}.}

\item{active}{Active channel names; \code{NULL} means all channels.}
}
\value{
An environment object with \code{reset}, \code{step}, \code{set_active}, \code{active_channels}.
}
\description{
Wraps a base environment whose \code{step} returns a named numeric \code{channels}
vector (and optional \code{base_reward}) and recomputes scalar reward as
\code{base_reward + sum(channels[active])}. \code{set_active()} selects which
reinforcement contingencies are in force, which is how the ABA instruments
manipulate the contingency on a Gymnasium-style environment.
}
\examples{
env <- contingency_env(fa_channel_env("escape"), active = "escape")
env$reset(seed = 0)
env$step(1)
}
