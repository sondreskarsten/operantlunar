% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{adhoc_fa}
\alias{adhoc_fa}
\title{Ad hoc functional analysis (the undisciplined pipeline)}
\usage{
adhoc_fa(
  true_function = "escape",
  arms = c("attention", "escape", "tangible", "goal"),
  n_sessions = 8L,
  n_seeds = 3L,
  keep = "first",
  seed0 = 0L,
  session_len = 100L,
  k = 10L,
  p_reinforce = 0.3,
  p_noise = 0.05,
  response_cost = 0.1
)
}
\arguments{
\item{true_function, arms, session_len, p_reinforce, p_noise, response_cost}{As elsewhere.}

\item{n_sessions}{Fixed session count (exogenous stopping).}

\item{n_seeds}{Seeds run.}

\item{keep}{Collapse rule: "first", "best", "majority", or "pool".}

\item{seed0}{Seed offset.}

\item{k}{Verdict window (capped at \code{n_sessions}).}
}
\value{
A single verdict string.
}
\description{
Deliberately reproduces the researcher degrees of freedom the protocol removes:
a fixed (often too small) number of sessions with no steady-state check
(exogenous stopping), a handful of seeds, and an arbitrary rule for collapsing
them to one verdict (\code{first} seed, \code{best} of K by differentiation magnitude,
\code{majority} vote without a reliability check, or \code{pool} which averages rates
across subjects). Its verdict is sensitive to these knobs; that sensitivity is
the point of comparison.
}
