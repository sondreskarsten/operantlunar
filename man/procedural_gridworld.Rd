% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedural.R
\name{procedural_gridworld}
\alias{procedural_gridworld}
\title{Procedural-layout gridworld (env_seed sets the apparatus)}
\usage{
procedural_gridworld(
  env_seed = 1L,
  size = 5L,
  arms = c("attention", "escape", "tangible", "goal"),
  n_walls = 4L,
  slip = 0.05,
  magnitude = 1,
  step_cost = 0.04
)
}
\arguments{
\item{env_seed}{Seed fixing the layout (the apparatus).}

\item{size}{Grid side length.}

\item{arms}{Channel names, one source each.}

\item{n_walls}{Number of wall cells.}

\item{slip}{Probability an action is replaced by a random one.}

\item{magnitude}{Source reinforcement magnitude.}

\item{step_cost}{Cost per executed move (stay is free).}
}
\value{
A channel environment with \code{reset}, \code{step}, \code{n_actions}, \code{arms}, \code{n_states}, \code{source_cell}, \code{start}, \code{walls}, \code{size}.
}
\description{
A navigation environment whose layout — start cell, the location of each
channel's reward source, and the walls — is fixed by \code{env_seed}. This makes
the env_seed a genuine setting variable (the apparatus / contours), distinct
from an agent's exploration seed (the subject). Reaching a source terminates
the episode and emits that source's channel (gated by \code{\link[=contingency_env]{contingency_env()}});
moving costs \code{step_cost}; a stay action costs nothing, so under a no-channel
control the agent's best policy is to stay and reach no source. Navigation is
slow to learn, which is the point: it makes "trained too little" a real
source of unreliable conclusions. The channels are the candidate functions —
the agent's own reward sources, not an imported four-function taxonomy.
}
