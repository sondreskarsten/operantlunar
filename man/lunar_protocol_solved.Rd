% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lunarlander.R
\name{lunar_protocol_solved}
\alias{lunar_protocol_solved}
\title{Protocol "is it solved?" verdict with quantified reliability}
\usage{
lunar_protocol_solved(
  returns = lunar_returns(),
  policy_seed = 0L,
  threshold = 200,
  n_boot = 1000L,
  block = 10L,
  min_blocks = 7L,
  k = 6L,
  tol_trend = 8,
  tol_bounce = 15
)
}
\arguments{
\item{returns}{Returns tibble.}

\item{policy_seed}{Policy to evaluate.}

\item{threshold}{Solved threshold.}

\item{n_boot}{Bootstrap resamples.}

\item{block, min_blocks, k, tol_trend, tol_bounce}{Steady-state parameters.}
}
\value{
A list with \code{verdict}, \code{estimate}, \code{stable}, \code{n_episodes}, \code{bootstrap_solved_fraction}.
}
\description{
Reads the steady-state estimate over all terrains, applies the frozen solved
threshold, and quantifies the residual uncertainty by bootstrapping the
terrain sample: the fraction of resampled means that clear the threshold is a
direct confidence readout. The verdict does not depend on an episode budget or
a terrain pool.
}
