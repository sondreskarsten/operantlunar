% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{stability_reached}
\alias{stability_reached}
\title{Steady-state stability of a per-condition rate series}
\usage{
stability_reached(rates, k = 10L, tol_trend = 0.1, tol_bounce = 0.25)
}
\arguments{
\item{rates}{A tibble with \code{session}, \code{condition}, \code{rate}.}

\item{k}{Window length (most recent sessions).}

\item{tol_trend}{Maximum allowed half-split mean difference within the window.}

\item{tol_bounce}{Maximum allowed range within the window.}
}
\value{
\code{TRUE} if every condition is stable over the last \code{k} sessions.
}
\description{
A Sidman-tradition stability criterion applied to the last \code{k} sessions of
each condition's response-rate series: stable when there is no trend (the
first-half and second-half means of the window differ by at most \code{tol_trend})
and bounce is bounded (the window range is at most \code{tol_bounce}), for every
condition. This is the endogenous stopping rule that replaces an arbitrary
fixed number of sessions.
}
