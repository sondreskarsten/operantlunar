% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procedural.R
\name{fa_subject_gridworld}
\alias{fa_subject_gridworld}
\title{One functional-analysis subject on the procedural gridworld}
\usage{
fa_subject_gridworld(
  true_function = "escape",
  arms = c("attention", "escape", "tangible", "goal"),
  env_seed = 1L,
  agent_seed = 1L,
  n_sessions = NULL,
  episodes_per_session = 20L,
  min_sessions = 25L,
  max_sessions = 70L,
  k = 10L,
  tol_trend = 0.12,
  tol_bounce = 0.3,
  max_steps = 40L,
  size = 5L,
  n_walls = 4L,
  slip = 0.05,
  step_cost = 0.04
)
}
\arguments{
\item{true_function}{Hidden maintaining channel.}

\item{arms}{Channels.}

\item{env_seed}{Layout seed (apparatus).}

\item{agent_seed}{Subject seed.}

\item{n_sessions}{If non-NULL, run exactly this many sessions (exogenous stopping); else steady-state.}

\item{episodes_per_session}{Episodes per condition per session.}

\item{min_sessions, max_sessions}{Session bounds for steady-state mode.}

\item{k}{Stabilised-window length.}

\item{tol_trend, tol_bounce}{Stability tolerances.}

\item{max_steps}{Step cap per episode.}

\item{size, n_walls, slip, step_cost}{Environment parameters.}
}
\value{
A list with \code{verdict}, \code{stable}, \code{n_sessions}, \code{rates}, \code{last_k}, \code{detail}.
}
\description{
The gridworld analogue of \code{\link[=fa_subject]{fa_subject()}}: a single subject (one \code{agent_seed})
at a fixed \code{env_seed} (the apparatus held constant, as a real functional
analysis holds the chamber constant across replications). Conditions (each
channel active, plus a play control) alternate by session; each session's data
point is the proportion of episodes that reach the target source (the source
tied to \code{true_function}). Reaching the target is reinforced only when its
channel is active, so the target-reach rate is elevated only under the
maintaining condition. Sessions are added until steady state or \code{max_sessions};
the verdict uses the criterion-line rule.
}
