% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signatures.R
\name{fi_chamber}
\alias{fi_chamber}
\title{Fixed-interval chamber with an elapsed-time observation}
\usage{
fi_chamber(interval = 20, magnitude = 1, response_cost = 0.02, cap = NULL)
}
\arguments{
\item{interval}{Fixed interval in steps.}

\item{magnitude}{Reinforcement magnitude.}

\item{response_cost}{Cost per response.}

\item{cap}{Maximum elapsed value exposed as the observation.}
}
\value{
An environment object with \code{reset} and \code{step}.
}
\description{
A single-operandum FI schedule that exposes time since the last reinforcer as
the observation, so a value agent can bring responding under temporal control.
Actions: 1 = respond, 2 = withhold.
}
\examples{
ch <- fi_chamber(20)
ch$reset(seed = 0)
ch$step(1)
}
